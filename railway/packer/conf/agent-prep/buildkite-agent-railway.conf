# Installed as /etc/systemd/system/buildkite-agent.service.d/railway.conf.
# Wants, not Requires: if prep fails the agent still starts, it just clones cold.
[Unit]
Wants=railway-agent-prep.service
After=railway-agent-prep.service
