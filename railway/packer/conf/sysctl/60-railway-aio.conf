# Seastar services (Scylla, redpanda) reserve AIO contexts per shard at startup
# from one host-wide pool. At the kernel default of 65536, two compose stacks on
# one agent exhaust it and the second fails with "Could not setup Async I/O".
# This is a cap, not an allocation.
fs.aio-max-nr = 1048576
