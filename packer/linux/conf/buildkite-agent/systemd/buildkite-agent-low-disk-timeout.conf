# The packaged unit ships TimeoutStopSec=0, which systemd reads as infinity,
# not as zero. Without a cap, stopping the agent blocks until its current job
# finishes, so a VM shutdown can hang indefinitely behind a long deploy.
[Service]
TimeoutStopSec=10min
